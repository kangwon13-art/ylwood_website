# 작업 컨텍스트 (감독 인수용 요약본)

> 이 문서는 2026-09-09에 전면 재작성되었습니다. 기존 상세 라운드별 로그(2026-07-06 ~ 2026-08-06, 221줄)는 `docs/context.md.old`에 그대로 보존되어 있습니다 — 특정 라운드의 세부 구현/버그 원인/검증 방법이 필요하면 그 파일을 참조하세요. 이 문서는 "새 세션에서 이 파일 하나만 읽어도 프로젝트 전체를 파악"하는 것을 목표로 한 구조화 요약본입니다.
>
> **재작성 사유에 대한 확인 참고**: 재작성 지시 시 "기존 context.md가 지시서#01 완료 시점에 멈춰있다"는 전제가 있었는데, 실제 확인 결과 옛 파일은 #01부터 최근 라운드(2026-08-06, PC 아코디언 세부 수정)까지 전부 기록되어 있었습니다(가장 오래된 항목이 파일 맨 앞에 있어 그렇게 보였을 가능성). 다만 "라운드별 로그가 계속 길어져 한눈에 파악하기 어렵다"는 문제의식 자체는 타당해 이번 재작성을 그대로 진행했습니다.

---

## 1. 프로젝트 개요

- **브랜드**: **INFILL**(by 영림우드) — 2026-09-14 지시서#07/#08로 "영림우드" 단독 브랜드에서 전환. 태그라인 "공간을 완성하는 인필". 사업자 정보·"영림몰딩도어 공식 유통사"·"영림카탈로그" 명칭 등 영림우드 관련 표기는 그대로 유지(엔도스먼트 관계, 상세는 3번/4번 참조).
- **목적**: 건축자재 실시간 단가 조회·견적·주문 사양 정리를 위한 정적 웹사이트. "by 영림우드"로 유통사 신뢰도를 유지하면서 INFILL을 전면 브랜드로 노출.
- **타겟 사용자**: 현장 소장, 시공업자, 자재 발주 담당자 — 즉시 단가를 확인하고 필요한 자재를 견적/문의로 연결하는 것이 핵심 사용 흐름.
- **저장소**: `https://github.com/kangwon13-art/ylwood_website` (브랜치: `main`)
- **정식 도메인**: `https://infill-wood.kr` (2026-09-15 구매·연결 완료) — Netlify DNS 사용, 네임서버 `dns1~4.p06.nsone.net`. **2026-09-16 `curl -I`로 3개 주소 실측 확인**: `infill-wood.kr`(200 OK, primary), `www.infill-wood.kr`(**301 → `https://infill-wood.kr/`**, 정상 리다이렉트), `endearing-cassata-cf0649.netlify.app`(**200 OK — 리다이렉트 아님**, apex와 완전히 동일한 콘텐츠를 별도 URL로 동시 서빙 중, Content-Length 동일 확인). 즉 "netlify.app이 자동으로 301 전환된다"는 가정은 사실이 아니며, 정확히는 **www만 apex로 리다이렉트되고 netlify.app 기본 도메인은 독립적으로 계속 200 응답**하는 상태 — SEO 중복 콘텐츠 우려가 있다면 Netlify 대시보드에서 별도로 primary domain 강제 리다이렉트 설정이 필요.
- **호스팅**: Netlify + GitHub 연동 자동배포(대시보드에서 설정, 저장소 내 `netlify.toml` 등 별도 설정 파일 없음) — Netlify 대시보드 접근 권한이 없어 실제 반영 여부는 매번 육안/curl 확인 필요.
- **SEO**: `sitemap.xml`(6개 페이지 URL 등록, `bc31364`), 네이버 서치어드바이저 사이트 등록+소유확인 완료(HTML 파일 업로드 방식, `naverb69c5f03d3ba5cfdfe5bbb0862756fc7.html`, `5cdae6a`).
- **카카오 채널**: `http://pf.kakao.com/_LixnwX/chat` (6개 페이지 전부 동일 링크로 실연동 확인됨)
- **로컬 개발**: `uv run python serve.py`(포트 8080, `Cache-Control: no-store` 강제) + `cloudflared.exe`(portable quick tunnel)로 실기기 확인 링크 발급.

### 주요 파일 (현재 실제 링크되어 서비스 중인 6개 페이지)

| 파일 | 타이틀(2026-09-15 기준) | 역할 |
|---|---|---|
| `index.html` | INFILL \| 건축자재 실시간 단가표 & 맞춤 견적 시스템 | 메인 랜딩 — 히어로 검색, 카테고리 목록, 유통사 배지, 문의 폼 |
| `calculator.html` | (index와 동일 타이틀) | 실시간 자재 견적 계산기 — 전 카테고리 단가표+장바구니, 이 사이트의 핵심 기능 |
| `catalog.html` | INFILL \| 영림카탈로그 - 디지털 카탈로그 | E-카탈로그(PDF/이미지 형태 제품 카탈로그) 열람 페이지 |
| `door_order.html` | INFILL \| 문짝·문틀 주문 사양 정리 | 문/문틀 종류·색상·사이즈 조합 선택 후 카카오로 주문 사양 전달 |
| `molding_catalog.html` | INFILL \| 몰딩 색상별 주문 (구: 몰딩 색상별 재고 확인) | 몰딩 8개 색상별 재고 O/X 확인 + 장바구니 |
| `wallpanel.html` | INFILL \| 월판넬 디자인 둘러보기 | 템바보드/인피니월 패턴·색상 선택 후 장바구니 |

각 파일은 **완전히 독립된 `<style>`/`<script>` 블록**을 가진 개별 정적 페이지입니다(공유 CSS/JS 파일 없음) — 한 페이지 수정이 다른 페이지에 영향을 줄 수 없는 구조이며, 동시에 6개 페이지에 걸친 공통 컴포넌트(헤더, 담기 버튼 등)는 매번 개별적으로 동일하게 반영해야 합니다.

### 저장소 정리 이력 (2026-09-09)
아래 파일들은 사이트 내비게이션 어디서도 링크되지 않는 과거 백업본/구버전 시안/중복본으로 확인되어 감독 승인 후 삭제 완료했습니다:
- `index_restored.html`, `admin.html`, `admin_restored.html` — 미사용 백업 HTML (`a5370b9`)
- `docs/design.md` — 이 프로젝트와 무관한 내용(Pinterest 디자인 시스템 참고 문서로 추정), untracked 상태로 저장소에 남아있던 것 (`a5370b9`)
- `yl181.PNG`, `yl182.PNG`, `yl40.PNG`, `yl50.PNG`, `yl53-1.PNG` — 루트 중복본(`colors/` 정본과 MD5 완전 일치 확인 후 삭제, `colors/` 쪽만 유지) (`4309fdc`)
- `LOGO1.png`, `logo.png` — 브랜드 로고 확정(2026-08-04) 이전의 구버전 시안, 현재 코드 어디에서도 미참조 확인 후 삭제 (`4309fdc`)

같은 날, 이 저장소와 무관하게 로컬 디스크에만 존재하던 **오래된 중복 clone 2곳도 정리**했습니다(git 커밋 대상 아님, 로컬 파일시스템 삭제): `C:\Users\1\Desktop\ylwood_website`(HEAD `924a4b6`, `b2cdbc2` 이후 이력 누락된 구버전), `C:\Users\1\ylwood_website`(HEAD `4014a46`, 2026-07-02 초기 상태) — 둘 다 삭제 전 `git status`로 미커밋 변경사항 없음(clean) 확인 후 삭제. 앞으로 이 프로젝트는 `C:\Users\1\Desktop\영림\영림우드 자료\영림 사이트 폴더` 한 곳만 사용합니다.

**유지 결정**: `temba_board.png`(루트)는 실제 wood slat 벽면 인테리어 스톡사진이지만, wallpanel.html이 요구하는 `wallpanel/temba/템바보드_직각대형.jpg` 등 7종 변형별 개별 파일과는 폴더 위치·파일명·매수가 전혀 다름 — **범용 스톡사진을 특정 제품 변형 사진으로 대체 사용하면 실제 제품과 다른 이미지가 노출되어 발주 오류 소지가 있다는 감독 판단으로, 이동/재활용하지 않고 참고용으로만 그대로 보관** 중입니다(사이트 어디에도 미사용).

### 저장소 내 미사용/백업 파일 (서비스 링크 없음, 삭제 안 함)
현재는 위 정리 후 해당 없음 — 남아있는 것은 `temba_board.png`(참고용 보관, 위 설명 참조) 뿐입니다.

### 재단 최적화 도구 (별도 서브프로젝트, 감독 라인 밖)
`cutting/` 폴더 — "재단 최적화 프로그램"(`jaedan_app.html` 등). 감독 확인 결과 이 사이트와는 완전히 별개 폴더/기능이며(원격 저장소에 직접 업로드된 것으로 보이고 이번 감독 세션의 작업 대상이 아니었음), 최근 커밋(`924a4b6`, `2023d07` 등)도 이 폴더 안에서만 발생했습니다. 본 문서의 범위에서는 제외합니다.

---

## 2. 완료된 지시서 이력 (전체)

| 지시서 | 완료일 | 커밋 해시 | 핵심 변경 |
|---|---|---|---|
| #01 CSS/스크립트 중복 정리 | 2026-07-09 | `438786b` | 사이드바 4중 선언 통합, 모바일 단가표 레이아웃 병합, 죽은 CSS 삭제 |
| #02 라이트 인더스트리얼 + 계산기 퍼스트 | 2026-07-09 | `4e64a56` (+후속 `8d2a6bd`) | 다크→라이트 테마 전환, 히어로→계산기→카테고리 순서 재배치, 견적서 html2canvas PNG 저장 기능 신규 |
| #03 모바일 2단 구조(그룹 카드+옵션 바텀시트) | 2026-07-10 | `3f61e30`(MDF 시연) → `f4f6b70`(전 카테고리 확산) | 340개 품목→65개 그룹 매핑(`g1-group-mapping-v2.md`), 그룹 카드 목록+옵션 바텀시트 UI, 전 카테고리(운반비 제외) 적용 |
| #04 Netlify 자동배포 | (커밋 없음 — 대시보드 설정) | — | GitHub-Netlify 연동으로 push 시 자동 배포. 저장소에는 별도 설정 파일이 없어(Netlify 대시보드 측 설정) 코드 이력에는 남지 않음 |
| #05 페이지 분리 + 카카오 실연동 | 2026-07-11 | `a112420` (+후속 `9685490`, `e19115c`, `2fb8ad1`) | `calculator.html`/`catalog.html` 분리, 카카오톡 채널 연동, 풀블리드 레이아웃 확정, 헤더/히어로 정리, 재발 방지 원칙 7건 `decisions.md`에 문서화 |
| #06 코드 정리(1600줄+ 삭제) + 데이터 안전성 | 2026-07-13 | `1475a5b` | sessionStorage/null 참조 방어, 병합 로직 정리, 카드형 레이아웃 전환 |

**#01~#06 이후, 감독 라인 안에서 계속된 후속 작업** (번호 없이 개별 지시서로 진행됨):

| 완료일 | 커밋 | 내용 |
|---|---|---|
| 2026-07-23~24 | `dd0080b` | calculator.html UI 종합 개편(담기/카트 분리, PC 사이드바, 정렬 화살표, PC 카트 오버레이, 표 컬럼 고정폭 등) — 4페이지(door_order/wallpanel/molding_catalog 포함) 공통 규칙(수량 직접입력, 6개 헤더 내비) 통일 |
| 2026-09-03 (`b2cdbc2`) | `b2cdbc2` | 브랜드 로고 시스템 6페이지 적용, calculator.html 구글시트 연동 근본 버그 수정(헤더 인덱싱 오류로 시트 데이터가 한 번도 반영된 적 없었던 문제), index.html 히어로 "대산(daesan.ai)" 참고 리브랜딩, index/calculator PC·모바일 헤더 폭 조정, calculator.html PC 소그룹 인라인 아코디언 도입 등 — **2026-08-04~09-09 사이의 모든 미커밋 작업분이 이 한 커밋에 포함되어 오늘 push 완료됨** |
| 2026-09-14, 지시서#07 | (커밋 없음, 조사 전용) | **브랜드 전환 전수 조사** — "영림우드" 등장 위치 전수 grep, 로고 자산·헤더 구조·외부 연동(EmailJS/카카오/견적서 PNG) 조사. 코드 변경 없이 감독 검토용 표 5종 산출. |
| 2026-09-14, 지시서#08 | `e318acd` | **INFILL 브랜드 전환 실행** — 6개 페이지 헤더 육각 심볼(네이비 프레임+오렌지 판재면) 인라인 SVG로 교체(6곳 개별, 공유 파일 아님), 워드마크 "INFILL"+"by 영림우드"(index.html만 태그라인 "공간을 완성하는 인필" 추가), PC 헤더 우측 대표전화 신규 추가(더미값 02-1234-5678), `<title>`/`og:title`/푸터로고/카카오전송문구/견적서PNG 로고·파일명 등 브랜드명 A분류 텍스트 일괄 교체, 파비콘·앱아이콘 재생성. "영림 카탈로그"·사업자 정보·"영림몰딩도어 공식 유통사"는 확정에 따라 그대로 유지. |
| 2026-09-14 후속 | `7b23208` | 탭 명칭 변경(6개 페이지 nav+index 히어로 카드): "몰딩 재고 확인"→**"몰딩 주문"**, "월판넬"→**"월판넬 주문"**. index.html 모바일·태블릿 헤더에 PC와 동일한 태그라인 추가(실측 기반, 375px에서도 292px/343px 여유로 줄바꿈 없음 확인). |
| 2026-09-14 후속 | `63a6a90` | index.html 히어로 서브카피("실시간 단가와 즉시 견적. INFILL 공식 유통.") 삭제 — 브랜드 치환 후 문법상 어색해진 문구 제거, `.hero-title` margin-bottom 20→32px로 여백 보정. |
| 2026-09-15 후속 | `5cdae6a` | 네이버 서치어드바이저 소유확인 파일(`naverb69c5f03d3ba5cfdfe5bbb0862756fc7.html`) 저장소 루트에 추가, 내용 무수정. |
| 2026-09-15 후속 | `bc31364` | `sitemap.xml` 신규 추가(6개 페이지 URL, `https://infill-wood.kr` 기준). |
| 2026-09-15 후속 | `e5cee02` | 6개 페이지 푸터 사업자 정보를 더미값에서 실제 값으로 교체(상호/대표이사/사업자등록번호/주소/이메일/팩스) — 상세는 4번 섹션 참조. |
| 2026-09-15 후속(긴급) | `c5b46d2` | **긴급 버그 수정** — 품목 id 중복으로 엉뚱한 품목이 장바구니에 담기는 사고(`ensureUniqueIds()` 안전장치 추가). 상세는 8번 섹션·`decisions.md` 원칙 8 참조. |
| 2026-09-15 후속 | `7bd7e20` | 위 id 중복 사고의 재발 방지 원칙(`decisions.md` #8)·해결 이력(본 문서 8번 섹션) 문서화. |
| 2026-09-16, 지시서#10 A | `56de3e9` | Netlify 기본 도메인(`endearing-cassata-cf0649.netlify.app`) → 커스텀 도메인 `infill-wood.kr` 301 강제 리다이렉트. 저장소 루트에 `_redirects` 파일 신규(경로·쿼리스트링 보존, Deploy Preview URL은 영향 없음, `infill-wood.kr` 자체는 루프 없이 200 유지 — 배포 후 curl로 검증 완료). |
| 2026-09-16, 지시서#10 B | `a6f6592` | **calculator.html 이메일 견적 요청 폼 신규**(카카오 병행 채널, 대체 아님) + `catalog.html` 죽은 EmailJS SDK 스크립트 제거. 상세는 4번 섹션 참조. |
| 2026-09-18, 지시서#12 4차(진행중) | `4231a19`(**로컬 커밋만, push 안 됨**) | **인테리어합판 7그룹 추가**(`groups/interior-plywood-*.html`, 25개 품목). 레드오크합판 1품목 단독 그룹도 합판과 동일 판단으로 개별 페이지 유지. |
| 2026-09-18, 지시서#12 3차 | `d336cb8`(push 완료, `93570ca`에 포함) | **합판 9그룹 추가**(`groups/plywood-*.html`, 35개 품목). 백색 코팅합판/미장(라미날 합판)/스페이스월 3개 단독 그룹은 감독 판단(딥링크 일관성+SEO 가치)으로 통합 없이 개별 페이지 진행. |
| 2026-09-18, 지시서#12 2차 | `157cfcd`(push 완료, `04cec1c`에 포함) | **MDF 1그룹 추가**(`groups/mdf-general.html`, 시트 그룹명 "MDF" 단일). 12개 품목 전부 포함, 9T/12T/15T 고밀도 변형은 시트 NO 순서상 뒤로 몰려 표시(calculator.html과 동일 순서, 감독 확인하에 유지). |
| 2026-09-18, 지시서#12 1차 | `8c9ea73`(push 완료, `04cec1c`에 포함) | **그룹 정적 페이지 확산 1차 — 마감자재 3그룹 + g1 문서 전수 감사.** 9개 카테고리 전체를 시트 실제 그룹값과 대조해 8개 카테고리 정정, 철물/부자재는 시트 정리 전까지 보류. `groups/finishing-{aquaboard,stone-flexible,wallsystem}.html` 신규(영림 월판넬은 전품목 견적문의라 문의유도형 템플릿). 상세는 4번 섹션. **배포 규칙 변경**: 크레딧 소모 확인돼 카테고리 2~3개 분량을 모아 감독 배치 승인 후 일괄 push하는 방식으로 전환(7번 참조). |
| 2026-09-16, 지시서#11 Part B(파일럿) | `4819af3` | **그룹별 정적 URL 파일럿** — 석고보드 4개 그룹(`groups/plaster-{general,waterproof,fireproof,moisture}.html`) 신규. `scripts/generate_group_pages.py`(구글시트 CSV를 로컬에서 읽어 정적 HTML로 굽는 생성 스크립트, 빌드 없는 배포 원칙 유지), `docs/group-slug-map.md`(그룹↔슬러그 매핑), `sitemap.xml` 4개 URL 추가, `decisions.md` 원칙 9(시트 단가 수정 시 그룹 페이지 재생성 요청) 추가. 상세는 4번 섹션 참조. |

> 참고: 위 표의 "완료일"은 문서(`context.md.old`)에 기록된 작업 수행일이고, 괄호 병기한 날짜는 실제 git 커밋 시각입니다. 여러 라운드가 미커밋 상태로 쌓여 있다가 감독 실기기 확인 후 한 번에 커밋되는 경우가 있어 두 날짜가 다를 수 있습니다.

---

## 3. 감독 라인 밖에서 만들어진 페이지 (3개)

아래 3개 페이지는 번호가 매겨진 지시서(#01~#06) 계열이 아니라, 그때그때 개별 요청으로 신규 제작되었습니다. **감독 지시 이력: 없음 — 감독 라인 밖에서 만들어진 페이지**(우선순위가 낮아서가 아니라, 감독이 그 시점에 이 페이지들의 제작 자체를 지시하지 않았다는 의미). **원칙: 앞으로 이 3개 페이지도 감독 지시 대상에 정식으로 포함합니다.**

| 페이지 | 감독 지시 이력 | 최초 커밋 | 목적/기능 | 현재 상태 |
|---|---|---|---|---|
| `door_order.html` | 없음 — 감독 라인 밖에서 만들어진 페이지 | `79a6149` (2026-07-17) | 문 종류·문틀 종류(5종)·바수·사이즈·색상(13종+미정)·문지방/열리는 방향을 카드형 단일 스크롤 UI로 선택 → 세트 담기/삭제(저장 방식: `sessionStorage`, 키 `doorOrderSets` — `localStorage` 사용 없음) → 카카오톡으로 사양 전달. 구글시트 연동 없이 전부 하드코딩 데이터. | 안정적으로 운영 중. 색상 스와치 이미지 로딩 실패 시 재시도+플레이스홀더 로직까지 보강 완료(2026-07-24, `dd0080b`에 포함되어 커밋됨). |
| `molding_catalog.html` | 없음 — 감독 라인 밖에서 만들어진 페이지 | `929c778` (2026-07-18) | 몰딩 8개 색상별 재고를 스와치 선택 → 목록(재고 O/X) → 카트 → 다른 색상 문의 모달 흐름으로 제공. 구글시트 실연동(색상별 8개 탭, gid 매핑 방식). | 4차례 레이아웃 수정 끝에 안정화(`45b751f`, `6078b52`). |
| `wallpanel.html` | 없음 — 감독 라인 밖에서 만들어진 페이지 | `2b93162` (2026-07-20) | 월판넬(템바보드+인피니월) 라인별 형태→타입→색상 단계 선택 → 담기 → 카카오 전달. `steps` 배열 기반으로 향후 라인 추가가 쉬운 구조. | 담기 활성화 조건 검증, 수량 스테퍼 등 후속 보완 완료. 이미지 자산(`wallpanel/temba/*.jpg`) 일부가 아직 없어 "사진 준비중" 플레이스홀더로 대체 노출 중(기존 이슈, 미해결). |

---

## 4. 확정된 UI/UX 방향

- **컬러**: 라이트 테마 기준. 브랜드 오렌지 `--color-accent-orange: #FF6B35`(로고 적용 시 지시서의 `#E8663C` 대신 기존 값 유지로 확정), 카카오 옐로 `#FEE500`.
- **레이아웃(대산 스타일 카드형, index.html 히어로 기준)**: 좌우 12~20px 여백, 요소 간 gap 8~10px, 카드 내부 padding 14~16px, 헤더는 둥근 카드 형태(24px radius)로 상단에 여백을 두고 떠 있는 형태(PC 기준).
- **검색창/카테고리 탭(calculator.html 모바일+태블릿 기준, `decisions.md` 확정)**: 풀블리드(좌우 0px, 100vw), 내부 텍스트는 좌측 44px(아이콘)/우측 16px padding 유지, 카테고리 탭 래퍼도 풀블리드 + 내부 좌우 16px padding으로 첫/마지막 탭 여백 확보. **헤더는 풀블리드 대상 아님**(기존 여백 유지).
- **카테고리 순서**: "전체"가 기본 활성 탭 → 마감자재 → MDF → 합판 → 인테리어합판 → 목재/구조재 → 석고보드 → 단열재 → 방부목/특수목/합성목 → 루바/집성판 → 철물/부자재 → (운반비, 그룹 UI 예외로 플랫 테이블 유지).
- **그룹 매핑(65개 그룹, `g1-group-mapping-v2.md` 확정본)**: 예) 석고보드 12개 품목 → 일반석고보드/방수석고보드/방화석고보드/방균·차음 4그룹 + 석고본드 단독 1개. 합판 4x8(일반)은 그룹 내부 두께 오름차순, 구조재는 규격 오름차순 정렬 확정.
- **PC/모바일 분기 기준**: PC는 **1025px 이상**, 모바일+태블릿은 **1024px 이하**(768px 이하와 769~1024px을 별도 구분 없이 동일 취급하는 것이 기본 방침, `decisions.md` 원칙 1·7).
- **calculator.html PC 전용(2026-08-06 확정)**: 소그룹은 오버레이가 아닌 **카드 바로 아래 펼쳐지는 인라인 아코디언(하나만 열림)**, 세부 품목 행은 한 줄 표기(품명·규격 통합)로 우측 끝까지 채우고 단가/담기 묶음은 우측 여백 최소화. PC 검색 결과도 모달 대신 이 아코디언을 자동으로 열어 하이라이트.
- **브랜드 헤더(2026-09-14 확정, 지시서#08)**: 심볼=육각 프레임(네이비 `#1A1F2B` stroke)+적층 판재(오렌지 `#FF6B35` 채움 사각형+흰 가로줄 2개), 워드마크="INFILL"(자간 넓게)+"by 영림우드"(작은 회색). **index.html만** 태그라인 "공간을 완성하는 인필"과 구분선 추가(PC/모바일/태블릿 전부, 실측상 여유 충분해 폰트 축소 없이 그대로 적용). **nav 있는 5개 페이지**(calculator/catalog/door_order/molding_catalog/wallpanel)는 좌측을 "심볼+INFILL+by영림우드"로 간결하게 유지(태그라인 생략) — nav 6개+PC 전화번호까지 우측에 들어가야 해서 좌측을 일부러 압축. 심볼은 6개 페이지 헤더에 **인라인 SVG로 각각 개별 존재**(공유 파일 아님 — 향후 심볼 재수정 시 6곳 전부 개별 수정 필요, `assets/logo/*.svg`는 참고용 최신 사본일 뿐 어디서도 `<img>`로 참조되지 않음).
- **PC 헤더 전화번호(2026-09-14 신규)**: PC(각 파일 기존 PC 분기점 기준)에서만 헤더 우측에 오렌지 원형 아이콘+전화번호 노출, 모바일/태블릿은 기존처럼 우하단 플로팅 버튼만. **현재 `02-1234-5678`은 더미값 — 실제 대표번호 개통 전까지 그대로 유지하기로 확정**(7번 백로그 참조).
- **탭/nav 명칭(2026-09-14 확정)**: "몰딩 재고 확인"→**"몰딩 주문"**, "월판넬"→**"월판넬 주문"**으로 6개 페이지 nav + index.html 히어로 빠른 링크 카드까지 전부 통일. molding_catalog.html은 `<title>`/`<h1>`도 함께 "몰딩 색상별 주문"으로 맞춤(nav-title 불일치 방지). "영림 카탈로그"/"영림카탈로그" 표기는 영림 본사 카탈로그를 그대로 게재하는 페이지라는 의미라 **교체 대상 아님(유지 확정)**.
- **사업자 정보(2026-09-15 확정, 6개 페이지 푸터 공통)**: 상호 `(주)영림우드`(기존 "종합자재" 표기 제거 — 감독이 최종 확정, 추가 수정 불필요), 대표이사 `허강원`, 사업자등록번호 `124-87-19164`, 주소 `경기도 수원시 권선구 서부로 1628`, 이메일 `ylwood0009@naver.com`, 팩스 `031-291-4629`. 영문 저작권 표기 `Younglim Wood Corporation`은 손대지 않음(이미 "종합자재" 미포함 형태라 확정 그대로 유지).
- **그룹별 정적 URL 파일럿(2026-09-16 확정, 지시서#11 Part B)**: 그룹당 하나씩 정적 랜딩 페이지(`groups/<슬러그>.html`)를 생성해 SEO(특히 JS 렌더링 지원이 약한 네이버 크롤러) 유입을 노리는 구조. 가격 데이터는 **생성 시점 스냅샷 방식**(클라이언트 재조회 방식 아님) — `scripts/generate_group_pages.py`가 calculator.html과 동일한 공개 구글시트 CSV를 읽어 로컬에서 정적 HTML을 굽고, 결과물을 그대로 커밋(빌드 없는 정적 배포 원칙 유지). 헤더 검증(필수 컬럼 누락 시 생성 중단)·가격 이상치 감지(직전 스냅샷 대비 ±20% 이상 변동 시 경고)·생성 시점 스냅샷을 HTML 주석에 남기는 3중 안전장치로 구글시트 헤더 인덱싱 버그(2026-08) 재발을 막음. **시트를 수정해도 자동 반영되지 않으므로, 단가 수정 시 그룹 페이지 재생성을 요청해야 함**(`decisions.md` 원칙 9). 각 그룹 페이지는 **셀프 canonical**(calculator.html로 canonical을 몰지 않음 — 포함 관계이지 중복 콘텐츠가 아니라는 판단) + GA4 계측 + JSON-LD(ItemList/Product) 포함. "담기/견적" CTA는 `calculator.html?cat=<카테고리>&id=<대표품목id>` 링크 하나로 처리 — calculator.html에 이미 있던 `handleUrlParameters()`/`openGroupSheetForItem()` 딥링크 로직을 그대로 재사용해 PC는 아코디언, 모바일은 바텀시트가 자동으로 열림(calculator.html 쪽 신규 코드 없음). 그룹명(한글)에는 안정적인 id가 없어 `docs/group-slug-map.md`에서 그룹마다 영문 슬러그를 수동으로 한 번 부여해 고정(자동 로마자 변환 사용 안 함). **주의**: `g1-group-mapping-v2.md` 문서상 "방균/차음"과 구글시트 실제 "그룹" 컬럼 값 "차음/방균보드"가 표기가 달라(시트 값이 코드에서 우선 적용됨) 슬러그 맵은 시트 값 기준으로 맞춤 — 오타인지 의도적 변경인지 확인 필요(8번 섹션 참조).
- **calculator.html 이메일 견적 요청 폼(2026-09-16 확정, 지시서#10 B)**: 카트에 품목이 1개 이상 있을 때만 사이드바에 "이메일로 견적 요청" 버튼 노출(외곽선 스타일로 카카오 버튼과 시각적 위계 구분, 카카오 버튼은 그대로 유지되는 병행 채널). 클릭 시 기존 `.option-sheet`와 동일한 전폭 바텀시트 패턴을 그대로 재사용한 별도 `.quote-request-*` 오버레이/시트가 열림 — **PC에서도 별도 중앙 모달을 만들지 않고 모바일과 동일한 바텀시트 재사용**(목업 3번째 프레임의 PC 중앙모달 안과는 다르게, 기존 사이트 관례인 "PC 전용 별도 디자인 지양"을 우선). 필드는 성함·연락처(필수)+현장명/지역·요청사항(선택) 4개만 — 항목이 많으면 이탈한다는 판단에 따라 최소화. 이메일은 index.html이 쓰던 `service_itchlia`/`template_6udhffm` 템플릿을 필드 매핑으로 재사용(신규 템플릿 생성 없음). **전송 성공 후에도 카트를 비우지 않음**(index.html의 1회성 문의 폼과 달리 calculator.html의 카트는 지속 사용 도구라는 판단, index.html 패턴과 의도적으로 다름). 필수값 미입력 검증은 네이티브 `required`가 아닌 `form novalidate`+커스텀 JS로 처리하여 필드별 한국어 에러 문구("성함을 입력해주세요" 등)를 표시(2026-09-16 후속 #10-1, 최초 구현은 네이티브 검증이라 브라우저 기본 말풍선만 뜨고 커스텀 에러 UI가 아예 실행되지 않는 문제가 있었음 — 원인 진단 후 수정). GA4 `generate_lead` 이벤트를 전송 성공 시 금액과 함께 발생.

---

## 5. 협업 원칙 (`decisions.md` 재발 방지 원칙 8건 요약 — 전문은 `docs/decisions.md` 참조)

1. **커밋 전 git status 전체 파일 목록 체크** — 작업 범위의 파일이 전부 스테이징됐는지 확인 후 커밋.
2. **커밋·push는 감독 명시적 승인 후에만** — "확인 완료"/"승인" 문구 없이 임의 진행 금지.
3. **여러 파일 지시 시 파일별 개별 반영 확인** — 유사 컴포넌트를 공유하는 다른 파일도 교차검증.
4. **로컬 서버 no-store + cloudflared 터널 우선 사용** — 실기기 확인은 항상 cloudflared 링크로.
5. **정렬·여백 검증은 픽셀 좌표 수치 필수** — `getBoundingClientRect()` 실측치 첨부, "비슷함" 판정 금지.
6. **인라인 style 우선순위 확인** — 스타일 미적용 시 `!important`보다 먼저 HTML 인라인 `style=` 존재 여부 확인.
7. **모바일+태블릿 풀블리드가 기본 방침** — 별도 명시 없는 한 두 브레이크포인트 모두 대상, PC(1025px+)는 별도 미디어쿼리로 격리.
8. **구글시트에 품목 추가 시 NO(순번) 컬럼 중복 금지** — 기존 시트 탭의 마지막 NO 다음 번호부터 이어서 부여할 것. 품목코드가 비어있으면 NO로 품목 `id`를 만드는 구조라, NO가 겹치면 서로 다른 품목이 같은 id를 가져 "A를 담았는데 B가 담기는" 사고로 이어짐(2026-09-15 실제 발생·수정, `c5b46d2`). 코드 쪽에도 `ensureUniqueIds()` 최후 방어선이 있지만, 애초에 NO를 안 겹치게 입력하는 게 원칙.

추가로 이번 세션(2026-08 이후)에서 확립된 운영 규칙:
- 매 라운드 종료 시 `docs/context.md` 자동 갱신.
- 완료 보고엔 실기기(cloudflared) 링크 무조건 포함.
- `taskkill`은 이름(`/IM`)이 아닌 PID(`/PID`)로만 — 감독의 실제 크롬 창이 함께 종료된 사고 이후 확정.
- 영림우드는 "영림몰딩도어" 브랜드의 공식 유통사 — 사이트 내 "영림몰딩도어" 문구는 오타 아님(별개 회사 관계).

### 과거 회귀·재수정 이력 (위 원칙 8건이 생긴 근거)

위 협업 원칙은 전부 실제로 발생했던 사고에 대한 재발 방지책입니다. 다음 세션에서도 같은 함정을 반복하지 않도록 사고 자체를 짧게 남겨둡니다.

- **index.html 커밋 누락 사고** (#04~#05 사이, 2026-07-11): index.html 변경사항이 여러 라운드 동안 커밋 사이클에서 누락되어 Netlify 실서버에는 옛 버전만 남아있던 사고가 실제로 발생 → 원칙 1(커밋 전 git status 전체 목록 체크), 원칙 3(여러 파일 지시 시 파일별 개별 반영 확인)의 근거.
- **인라인 style이 `!important`를 이기던 문제** (2026-07-11, 여백 정렬 라운드 반복): `.calculator-section`/`.global-search-container`의 인라인 `style=` 속성이 CSS 미디어쿼리의 `!important` 오버라이드를 전부 무력화해, 좌우 여백 정렬 작업이 여러 라운드에 걸쳐 반복 재발 → 원칙 6(인라인 style 우선 확인)의 근거.
- **정렬 육안 확인의 한계**: "비슷함"/"대략 맞음" 수준의 육안 판정으로 정렬 통과를 선언했다가 실기기에서 재차 오정렬이 발견되어 재작업이 반복 → 원칙 5(픽셀 좌표 수치 검증 필수)의 근거.
- **태블릿 브레이크포인트 누락 반복**: "모바일"이라고만 지시가 와도 실제로는 태블릿(769~1024px)에도 동일 적용이 필요한데 이를 누락하는 경우가 반복돼, "별도 명시 없는 한 모바일+태블릿 둘 다 대상, 풀블리드가 기본"이라는 원칙 7로 아예 기본값을 고정.
- **품목 id 중복으로 엉뚱한 품목이 담기던 사고** (2026-09-15): 구글시트 합판 탭에 품목 추가 시 NO(순번)를 이어서 매기지 않고 기존 값과 겹치게 입력해, "미장(라미날 합판)"을 담아도 "17.5mm 베트남 보급형"이 담기는 사고 발생 → 원칙 8(구글시트 NO 컬럼 중복 금지)의 근거.

---

## 6. 진행 중 준비물 (사장님 병행 작업)

- Google Sheets에 "그룹" 열 추가 — 현재는 시트에 그룹 열이 없어도 `GROUP_FALLBACK` 하드코딩 매핑으로 동작하지만, 시트에 그룹 열이 생기면 그쪽이 우선 적용되도록 이미 구현되어 있음.
- 제품 실사 사진 확충 — 특히 `wallpanel.html`의 템바보드 썸네일(`wallpanel/temba/*.jpg`) 일부가 현재 비어 있어 플레이스홀더로 대체 중. **실제 7종 변형(직각대형/반달대형/빗각/직각소형/반달소형/삼각/역반달)별로 각각 사진이 필요** — 2026-09-09 조사 결과 루트에 있던 `temba_board.png`(범용 wood slat 스톡사진)로는 대체 불가 확인(제품 변형별 실사가 아니라 발주 오류 소지, 사용 보류 결정). 범용 사진 재활용이 아니라 실제 촬영/구매 사진 확보가 유일한 해결책입니다.
- `catalog.html`(E-카탈로그) 자료 — 현재 페이지 구조는 있으나 실제 카탈로그 콘텐츠 최신화 필요 여부 확인 필요.

---

## 7. 남은 백로그

- **대표전화번호 개통 후 6개 페이지 일괄 교체** — 현재 헤더 신규 전화번호·푸터 "고객센터"·우하단 플로팅 버튼(`tel:`)·calculator.html 견적서 PNG 템플릿까지 전부 더미값 `02-1234-5678` 그대로 유지 중(2026-09-15, 감독이 "전화번호는 이번 라운드 보류, 개통 후 별도 지시"로 명시적으로 확정). 실제 번호 개통되면 6개 페이지 전수 교체 필요.
- PageSpeed/Lighthouse 최적화 — 배포 전이라 실행하지 못했던 이력이 있음(#02 라운드), 배포 후(현재 `infill-wood.kr` 정식 도메인 연결 완료) 재확인 가능해짐.
- PWA 홈 화면 추가 — 브랜드 로고 적용 시 만들어둔 `assets/logo/icon-192.png`/`icon-512.png`가 아직 manifest에 연결되지 않고 대기 중(2026-09-14 심볼 교체로 아이콘 자체는 INFILL 최신 버전으로 갱신됨).
- Delivery(운반비) 단가 갱신 확인 — 최근 구글시트 연동 근본 버그 수정으로 그동안 시트 편집이 실제로 반영되지 않았을 가능성이 발견됨(아래 8번 참조) → 전체 카테고리 단가가 최신 시트값과 일치하는지 감독 확인 필요.
- (완료됨, 참고용) 자체 도메인 연결 — `infill-wood.kr` 구매·Netlify DNS 연결 완료(2026-09-15). 더 이상 백로그 아님, 1번 섹션 참조.
- **지시서#12 — 그룹별 정적 URL 나머지 카테고리 확산 (진행 중)**: 파일럿(석고보드) 승인 후 착수. 전수 감사 결과 원래 추정한 "61개 그룹"은 옛 `g1-group-mapping-v2.md` 기준이었고, 실제 시트 그룹 수는 카테고리마다 달라져 있었음(8번 섹션 참조). 마감자재 3그룹+MDF 1그룹은 push 완료(`04cec1c`). 합판 9그룹(`d336cb8`)+인테리어합판 7그룹(`4231a19`)은 로컬 커밋 완료, push 대기. 남은 카테고리: 목재구조재(4)→단열재(6)→방부목특수목(6)→루바집성판(12) 순서로 1~2개씩 진행, 철물/부자재(27그룹+미지정12)는 시트 정리 후 별도 재개. **배포 규칙(2026-09-18 신규)**: Netlify 배포 크레딧이 push 횟수에 비례해 소모되는 것을 확인해, 커밋은 매 카테고리마다 자유롭게 하되 **push는 카테고리 2~3개치를 모아 감독의 "이번 배치 push 승인" 이후에만 일괄 실행**하는 방식으로 전환.
- **index.html 이동형 문의 버튼과 calculator.html 이메일 견적 요청 폼의 통합 여부 검토** — index.html의 "선택 품목으로 견적 문의하기" 버튼(`copyEstimateAndScroll()`)은 카트 내용을 sessionStorage에 담아 index.html의 `#inquiry-form-sec`로 이동시키는 기존 경로이고, calculator.html에 2026-09-16(지시서#10 B) 신규 추가된 "이메일로 견적 요청" 버튼은 calculator.html 안에서 바텀시트로 완결되는 별도 경로 — 현재 두 경로가 각자 존재하며 아직 통합/정리하지 않음(지시서#10 승인 시점에 "그대로 두는 게 맞다"고 확인된 백로그, 우선순위 낮음). 두 채널 모두 결국 동일한 EmailJS 템플릿(`service_itchlia`/`template_6udhffm`)으로 수렴하므로, 추후 여유 있을 때 이동형 버튼을 신규 바텀시트 폼으로 흡수할지, 두 경로를 그대로 병행 유지할지 감독 판단 필요.

---

## 8. 현재 미해결 항목

- **2026-09-15 기준 전부 커밋·push 완료.** 2026-09-09 "실기기 확인 완료" 승인 이후 쌓였던 모든 작업 — 2026-08-04~09-09분 일괄 커밋(`b2cdbc2`), 브랜드 전환(#07/#08), 탭 명칭 변경, 히어로 서브카피 삭제, 네이버 서치어드바이저 등록, sitemap.xml, 사업자 정보 교체, id 중복 긴급 버그 수정까지 전부 `main`에 반영·Netlify 배포 확인 완료(`e318acd`~`7bd7e20`, 2번 섹션 표 참조). 남은 항목은 **7번 백로그(대표전화번호 개통 대기)**와 아래 항목들뿐입니다.
  - 참고로 과거 지시문에 적혀 있던 "미커밋 3건"은 2026-07-24 라운드에서 발견·수정된 아래 항목이며, 수정 대상 파일까지 포함해 전부 **`dd0080b`**(2026-07-24, 실제 도입 커밋 — `git log -S`로 대조 확인)에 이미 포함되어 커밋된 상태였습니다(별도 조치 불필요):
    1. **`calculator.html`** — 담기 버튼 폭이 "담기"/"담김" 상태 전환 시 바뀌던 문제(`min-width`→고정 `width`), 배경색 남색→오렌지 계열, 유니코드 체크 문자→SVG 체크 아이콘으로 교체.
    2. **`calculator.html`** — 단위 셀("포 (25kg)" 등 긴 단위)이 고정폭 표에서 줄바꿈되던 문제 → `white-space:nowrap` + 단위 컬럼 폭 확대.
    3. **`door_order.html`** — 색상 스와치 이미지가 간헐적으로 깨져 보이던 문제 → `handleColorImgError()`로 캐시무효화 재시도(최대 2회) 후 실패 시 플레이스홀더 전환.
- **가장 중요한 확인 필요 사항**: 구글시트 연동 근본 버그(헤더 인덱싱 오류로 시트 데이터가 한 번도 실제로 반영된 적이 없었던 문제, 2026-08-05 수정)로 인해, 그동안 시트에서 단가를 수정한 이력이 있다면 이번 수정 이후 처음으로 실제 반영됩니다 — **현재 시트 내용이 최신 단가와 일치하는지 감독 확인 필요**.
- `wallpanel.html` 이미지 404(템바보드 썸네일 일부 누락) — 기존 이슈, 자산 확보 후 해결 예정.
- `docs/design.md`(Pinterest 디자인 시스템 참고 문서로 추정, 이 프로젝트와 무관한 내용)가 커밋되지 않은 채 저장소에 남아있음 — 필요 없으면 삭제, 필요하면 별도 용도 확인 후 처리 요망.
- **카트/상태 저장 방식 조사 결과(`localStorage`/`sessionStorage` grep, 6개 서비스 페이지 전체 대상)**: `door_order.html`(`doorOrderSets`), `wallpanel.html`(`wallpanelCartItems`), `molding_catalog.html`(`moldingCartItems`), `index.html`/`calculator.html`(`yw_cart`, `pendingEstimateText` 등) **전부 `sessionStorage`만 사용 — `localStorage` API 호출은 6개 페이지 어디에도 없음**(공용 기기 데이터 잔류 이슈 없음, 통일 작업 불필요). `localStorage.setItem/getItem` 호출은 서비스 링크가 없는 백업 파일(`admin.html`, `admin_restored.html`, `index_restored.html`)에만 존재하며, 이 3개는 이미 1번 섹션에 "미사용/백업 파일"로 분리 기재돼 있음. 참고로 index.html/calculator.html의 `console.warn("Failed to load cart from localStorage: ...")` 문구는 실제 호출과 무관한 복사 잔재 오기(실제로는 sessionStorage를 읽음) — 기능상 문제는 아니나 다음에 그 파일들을 손댈 때 로그 문구도 함께 정정 권장.
- **(해결됨) 2026-09-15, 긴급 버그 — 엉뚱한 품목이 장바구니에 담기는 사고**: `calculator.html`의 품목 `id`는 품목코드가 비어있으면 `NO`(순번)로 대체해서 만드는데, 합판 시트 탭에 품목 3개(백색 코팅합판/미장(라미날 합판)/스페이스월)를 추가하면서 NO를 이어서 매기지 않고 25/26/27로 되돌려 매겨, 기존 NO 25/26/27 품목과 완전히 동일한 `id`가 생성됨 — "미장(라미날 합판)" 담기를 눌러도 실제로는 "17.5mm 4x8 합판 베트남 보급형"이 담기는 사고로 이어짐. 사장님이 시트 NO를 35/36/37로 재정리했고, 코드에도 `loadLocalData()`에 `ensureUniqueIds()` 안전장치를 추가해 앞으로 NO/코드가 중복돼도 자동으로 유일한 id를 부여하고 `console.warn`으로 경고하도록 함(`c5b46d2`). 재발 방지 원칙은 `decisions.md` 원칙 8 참조 — **앞으로 시트에 품목 추가 시 NO를 기존 값과 안 겹치게, 이어서 부여할 것.**
- **(2026-09-16, 지시서#09) 조사 결과 2건**:
  1. **`index_yl.html`(영림 제품 라이브러리)은 저장소에 존재하지 않습니다.** 현재도, git 전체 이력상으로도 한 번도 커밋된 적이 없고, 6개 페이지 어디에도 이 파일을 가리키는 링크가 없습니다(당연히 #08 브랜드 전환 반영 여부도 해당 없음 — 애초에 존재하지 않는 파일이라 반영할 대상 자체가 없음). 감독이 이 파일의 존재를 전제로 지시하신 것이라면, 파일명·경로를 다시 확인해주시거나 실제 자료를 전달해주셔야 다음 작업이 가능합니다.
  2. **EmailJS는 3개 페이지에 SDK가 로드**돼 있지만 실제로 발송(`emailjs.send(...)`)까지 연결된 곳은 **`index.html` 한 곳뿐**입니다. `index.html`의 `#inquiryForm`(문의 폼) → `onsubmit="handleFormSubmit(event)"` → `emailjs.send("service_itchlia", "template_6udhffm", templateParams)`까지 코드 경로는 완전하게 연결되어 있음을 확인했습니다(실제 발송 자체는 진짜 문의 메일을 발생시키게 되므로 테스트 발송은 하지 않았고, 대시보드 측 서비스/템플릿 설정은 이전 라운드에서 사장님이 이미 확인·수정하신 상태). `calculator.html`/`catalog.html`은 EmailJS SDK 스크립트만 로드돼 있고(`catalog.html`은 `init()`조차 없음) 실제 사용하는 곳이 코드 어디에도 없는 **죽은 스크립트 로드**입니다 — 기능상 문제는 없으나(단순 미사용 리소스) 페이지 무게만 차지하니 다음에 그 파일들을 손댈 때 정리 권장.
- **(2026-09-16, 지시서#09, 미커밋) SEO/계측 정리**: 6개 페이지 `<head>`에 `<link rel="canonical">`(자기 URL, `sitemap.xml`과 완전히 동일한 형식 확인) + GA4(`G-B8ZLZEVV7E`) `gtag.js` 스니펫 삽입, 저장소 루트에 `robots.txt`(전체 허용+Sitemap 경로) 신규 생성. 6개 페이지 전부 개별 반영 확인(원칙 3) — `grep`으로 `G-B8ZLZEVV7E` 2회(스크립트+config 호출)/`canonical` 1회씩 전부 존재 확인, Playwright로 `gtag` 함수 정의·`dataLayer` 채워짐·콘솔 에러 0건까지 실측. GA4 실시간 보고서 유입 확인은 실서버 배포 후(=커밋·push 후) 감독 쪽에서 GA4 대시보드로 확인 필요(로컬 에이전트는 GA4 대시보드 접근 권한 없음).
- **(2026-09-18, 지시서#12) 진행 중 — g1-group-mapping-v2.md 전수 감사 결과, 카테고리별 상태**:
  - 마감자재/MDF/합판/인테리어합판/목재구조재/단열재/방부목특수목/루바집성판: 8개 카테고리 전부 원안과 시트 실제 그룹이 어긋나 있던 것을 확인, `g1-group-mapping-v2.md`를 시트 기준으로 정정 완료(상세 표는 그 문서 참조).
  - **철물/부자재는 미해결** — 문서상 10개 기능 그룹으로 재작업됐다고 되어 있으나 시트에는 반영 안 돼 27개 세부 그룹 + 그룹 미지정 12개 품목이 그대로 남아있음. **감독이 시트를 정리한 뒤 재개하기로 확인**(2026-09-18) — 코드 조치 불필요, 시트 편집 작업.
  - **배포 크레딧 이슈**: push 1회당 Netlify 배포가 크레딧을 소모하는 것을 이번에 확인 — 이후 커밋은 자유롭게 하되 push는 감독의 배치 승인 후에만 실행. 마감자재+MDF 배치(`04cec1c`)는 2026-09-18 감독 승인으로 push·배포 확인 완료(4개 URL 200, sitemap.xml 반영 확인). 합판(`d336cb8`)+인테리어합판(`4231a19`) 9+7=16그룹은 다음 배치 push 대상으로 로컬에만 있음.
- **(2026-09-16, 지시서#11 Part B) 파일럿 커밋·배포 완료(`4819af3`), 후속 확인 대기 중**:
  - 실서버 배포 후 canonical/GA4 최종 확인은 **감독이 직접 진행 예정**(로컬 agent 확인은 이미 완료했지만, 실제 `infill-wood.kr` 도메인 기준 재확인은 감독 몫으로 넘김).
  - 구글 서치콘솔 URL 검사·색인 생성 요청도 **감독이 자신의 구글 계정으로 직접 진행 예정**(로컬 agent는 구글 계정 접근 권한이 없어 대행 불가) — 대상 URL 4개는 `docs/group-slug-map.md` 참조.
  - `g1-group-mapping-v2.md`("방균/차음")과 구글시트 실제 그룹값("차음/방균보드") 표기 불일치 — 확인 필요(4번 섹션 참조).
  - 파일럿 안정화 확인되면 #12(나머지 61개 그룹 확산) 착수 — 7번 백로그 참조.
- **(2026-09-16, 지시서#10) 완료.** A: `_redirects` 배포 후 curl로 301/경로보존/쿼리스트링보존/Deploy Preview 미영향/`infill-wood.kr` 루프없음 전부 검증(`56de3e9`). B: calculator.html 이메일 견적 요청 폼 신규 + catalog.html 죽은 SDK 제거, Playwright 자동화 테스트(빈 카트 가드/필수값 검증/성공·실패 피드백/중복전송 방지/카트 유지/GA4 이벤트) 및 cloudflared 실기기 확인 후 커밋·push 완료(`a6f6592`). 후속 #10-1로 필수값 검증을 네이티브→커스텀 JS로 전환해 필드별 한국어 에러 문구 추가, 스크린샷 재검증 후 승인받음. 상세는 4번 섹션 참조. **이메일 템플릿(`template_6udhffm`)의 본문 텍스트/디자인 자체는 이번 라운드에서 사장님이 EmailJS 대시보드에서 직접 수정하신 부분으로, 코드 배포와 무관합니다** — 코드 쪽은 템플릿 변수(`company`/`phone`/`category`/`area`/`message`/`estimate_rows`)를 채워 넣는 역할만 하며 템플릿 자체를 소유하지 않음.

---

## 9. 다음 세션 시작 방법

새 세션에서 감독이 이 파일(`docs/context.md`) 내용을 대화창에 붙여넣으면, 프로젝트 개요·완료 이력·확정 UI 방향·협업 원칙·미해결 항목을 즉시 파악해 이어받을 수 있습니다. 특정 라운드의 상세 구현/버그 진단 내역이 필요하면 `docs/context.md.old`를 함께 참조하세요(라운드별 상세 로그는 그쪽에 원문 그대로 보존).
